# Configurações do RDS para Nextcloud
# Copie este arquivo para .env e preencha com os valores do seu RDS

# Endpoint do RDS (sem porta)
RDS_ENDPOINT=your-rds-instance.region.rds.amazonaws.com

# Credenciais do banco de dados
RDS_USERNAME=postgres
RDS_PASSWORD=your-master-password
RDS_DATABASE=postgres
# OU para banco dedicado:
# RDS_USERNAME=nextcloud
# RDS_PASSWORD=your-nextcloud-password  
# RDS_DATABASE=nextcloud

# Porta do PostgreSQL (padrão: 5432)
RDS_PORT=5432

# Configurações opcionais para Amazon EFS (persistência de arquivos)
EFS_DNS_NAME=fs-xxxxxxxxx
AWS_REGION=us-east-1